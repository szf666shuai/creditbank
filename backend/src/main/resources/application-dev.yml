spring:
  datasource:
    url: jdbc:mysql://localhost:3306/credit_bank?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: creditbank
    password: creditbank123

  data:
    redis:
      host: localhost
      port: 6379
      password: redis123456

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
