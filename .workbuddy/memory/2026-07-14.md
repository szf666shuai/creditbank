# 2026-07-14 工作日志

## 任务：从零讲解用户负责的代码（个人中心 + 管理员主页）
用户负责：三个角色个人中心（学员 profile/、企业 profile/enterprise/、管理员 profile/admin/，全部由用户实现）+ 三个主页（学员/企业/管理员 home/，AI 生成，用户不懂语法）。范围已与用户确认：前端 Vue/TS + 后端 Java 都讲；三个主页都讲；视图 + 它调用的 api/store/types/utils 一起讲。重要补充（用户 21:52 澄清）：用户负责的"前端功能，背后的后端逻辑也是用户负责"。因此后端**不另立为最后单独阶段**，而是与对应前端功能**成对讲解**（每个功能 = 前端 .vue + 它调用的 api/*.ts + 后端 Controller/Service/Mapper + SQL）。

## 已完成
- 代码定位：通过 router/index.ts、config/role-routes.ts、config/profile-menu.ts 确认角色 0/1/2 与目录归属。
- 第 1 批讲解（地基文件，已发聊天）：types/auth.ts、stores/auth.ts、utils/api.ts、utils/format.ts。
- 确认后端有 340 个 .java 源码（含 ProfileController、HomeController、controller/service/mapper 分层），Phase 6 后端讲解可行。

## 计划顺序
1. 地基（已完成第1批）
2. 学员个人中心（14 页面，分批）
3. 企业个人中心（9 页面）
4. 管理员个人中心（10 页面）
5. 三个主页（Student/Enterprise/Admin HomeView）
6. 对应后端 Java

## 待办
- 等待用户确认第1批理解后，继续第2批：学员个人概览 IndexView.vue + api/profile.ts + api/profile-dashboard.ts。

---

## 任务：界面设计答辩内容方案（D:\creditbank-1\答辩内容方案.md，暂不做 PPT 画布）

### ⚠️ 技术真实性核查（答辩防翻车，均有代码证据，用 Explore 子代理核查）
- **区块链**：非真链！仅 SHA-256 证书摘要写 blockchain_hash 字段 + 查库比对（LearningService.sha256/verify）。无区块/前序哈希/共识/合约，无 module.blockchain 模块。→ 口径写"哈希存证/防伪验真"，可提"预留演进联盟链"。
- **TRTC 线上面试**：真集成。后端 TlsSigApiV2(HmacSHA256)签发 UserSig(InterviewRtcService)，前端 trtc-sdk-v5 真开房。默认 trtc.enabled=false 需配密钥。
- **JWT 鉴权**：真有。JwtUtil + AuthInterceptor + UserContext(ThreadLocal) + AuthSupport(三端角色/数据归属校验) + BCrypt。非 Spring Security 全套。
- **架构**：单体（单 Maven 模块 credit-bank-platform，按包分层）。无 Nacos/Gateway/Feign，Docker 仅 mysql/redis/nginx。→ 不能吹微服务。
- **前端**：真 Vue3+TS+Vite+Element Plus+Pinia+Router，三端合一 SPA。注意 package.json 版本号异常(ts~6/vite^8/router^5)，答辩前 npm ls 核实。
- **Agent**：对话答疑+AI 简历生成真调 DeepSeek(LlmService)，注入学习画像上下文；学习路径仅问答式无独立算法。
- **Redis**：声明未使用！代码无 RedisTemplate/@Cacheable。口径写"已集成、预留缓存层"或补代码。

### 内容方案演进（当前 30 页，5 幕叙事，新粗野风）
- 主线「学习的图书馆，也是"健身房"」；三断点(脱节/无激励/难认证)→四抓手闭环。
- 第三幕新增 P20 全链路求职闭环、P21 教学体验(弹幕/打卡/评论/课件/证书)。
- 技术块重写：P24 亮点全景(6点)+ 深讲 P25 Agent(LLM)/P26 TRTC/P27 安全鉴权/P28 哈希存证验真。全部诚实口径。
- 已对齐：P23 架构(分层单体非微服务)、P18(哈希存证非上链)、第0/4节表格、演示脚本、组织模板。
- 待用户定：技术深讲 4 页全留 or 精选 Agent+TRTC；Redis 是否补真实缓存；4 人分工；P16/P20/P21/P26/P28 素材。确认后重建 Ardot 画布(旧 fileId 703733234611321)。

## 用户拍板（第 2 轮）
- 技术深讲**只留 2 页**（P25 Agent + P26 TRTC），JWT/哈希存证压进 P24 全景 → 全篇 30→**28 页**。
- Redis：**要补真实缓存代码**。已在 backend 落地：
  - 新增 `config/RedisConfig.java`：`RedisTemplate<String,Object>` + Jackson2JsonRedisSerializer(HomeResponse)（JavaTimeModule，关时间戳）。
  - `HomeService`：`getHomeData()` 改 cache-aside（key `home:data:v1`，TTL 5min，MISS 重建 + 日志），新增 `evictHomeCache()`。
  - `HomeController`：新增 `DELETE /api/home/cache`（admin 守卫，调 evictHomeCache）。
  - `mvnw -o compile` 通过（exit 0）。
- 内容方案 `答辩内容方案.md` 同步：P24 全景点 6 / 技术选型表 / 待确认项 Redis 行已对齐为"首页聚合数据缓存"。
- 下一步：用户给 4 人分工 + P16/P20/P21 素材后，重建 Ardot 画布（旧 30 页草稿 fileId 703733234611321 按新 28 页重排）。

## 任务：Ardot 画布逐页生成（最终 28 页已建完 ✅）
- 旧 30 页草稿已隐藏（visible:false），新建 28 张 slide 帧（root 12:2–12:29 映射 P1–P28，章节页 P4/P8/P13/P22/P28 为 ink 暗色）。
- 风格：新粗野风（cream #FFF9F0 底 / ink #1A202C 字 / 绿蓝粉紫黄配色 / 粗黑边 + 硬阴影 DROP_SHADOW offset 8,8 radius 0 ink），Noto Sans SC。
- 本 session 续建：P10 用户调研 → P11 八大模块矩阵 → P12 非功能四象限 → P14 功能全景(中心学分银行+8模块+三端) → P15 三端角色 → P16 界面展示(3 UI 占位) → P17 Agent(对话+3能力) → P18 哈希存证(流转图+凭证卡) → P19 积分&诚信 → P20 求职闭环(流转+面试房) → P21 教学体验(弹幕/打卡/证书) → P23 架构+选型 → P24 技术全景6点 → P25 Agent 深讲 → P26 TRTC 深讲 → P27 4人团队+协作 → P28 ink 收尾页。
- 关键修复：P18 二维码标签溢出父框(40px)，已放大父框高度 220→270 修正。
- 校验：capture_layout(problemsOnly) 抽测 P24、P27 均无布局问题；P18 潜在问题已修。
- 适配器经验：单次 batch_edit ≤17 ops 稳定，≥23 ops 会 ADAPTER_DISCONNECTED；必须单条串行、不并行。
- 画布 URL：https://ardot.tencent.com/file/703733234611321 （用户去替换 P16/P20/P21/P26 真实界面/面试/证书截图占位）
- 待办（非阻塞）：P16/P20/P21/P26 三处 UI/视频/证书为渐变占位，建议替换为真实项目截图/录屏。

## 任务：导出答辩 PPT（Ardot → 本地文件）
- 画布 fileId 703733234611321，导出规范 28 页 = P 系列节点 12:2–12:29（S 系列 2:x 为旧草稿，未导出）。
- 用 `export_nodes` 逐批导出 28 张 PNG（2x，1920×1080@2x=3840×2160），落 `D:\creditbank-1\deck-export\png\`。
- 用 Python(Pillow) 按逻辑顺序 P1→P28 合并为单文件 PDF：`D:\creditbank-1\deck-export\星秩存册-界面设计答辩.pdf`（28 页 / 8.1 MB，校验 %PDF-1.4 有效）。
- 说明：Ardot 无原生 .pptx 导出；交付 PDF（可演示/打印）+ PNG 素材（可拼真实 .pptx）。若用户确需 .pptx 走 pptx skill 重建。

## 任务：导出 .pptx（图片型，可演示）
- 用 python-pptx 把 28 张 2x PNG 封装为 `D:\creditbank-1\deck-export\星秩存册-界面设计答辩.pptx`：13.333×7.5in(16:9 满版)、28 页、每页一张高清图、5.6 MB。校验通过（28 图全部嵌入）。
- 性质：图片型 pptx（版式/配色与画布一致，文字栅格化不可直改）。如需文字可编辑的原生 pptx，需按内容方案逐页矢量重建（工作量大），待用户确认再走。
